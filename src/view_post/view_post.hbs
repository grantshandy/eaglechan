<!DOCTYPE html>
<html lang="en">
<head>
	<title>{{ post.title }}</title>
	<meta charset="UTF-8" />
	<link rel="stylesheet" type="text/css" href="./styles.css" />
	<meta name="viewport" content="width=device-width,initial-scale=1" />
</head>
<body>
	<div class="min-h-screen space-y-2 p-3">
		<div class="w-full space-y-1">
			<div class="flow-root">
				<div class="float-left">
					<h1 class="text-2xl font-semibold">Viewing Post "{{ post.title }}"</h1>
					<p class="text-sm italic">you are user {{ user_id }}</p>
				</div>
				<a class="float-right underline" href="/">back to home</a>
			</div>
			<hr>
		</div>
		<div class="space-y-3">
			<p class="text-sm italic">Created by {{ post.user_id }} on {{ post.created }}.</p>
			<p>{{ post.content }}</p>
			<hr>
			<h3 class="text-lg font-semibold">Comments</h3>
			<form action="/comment-{{ post.post_id }}" method="post">
				<textarea class="pl-2 bg-gray-300 rounded-md w-2/3" name="content" rows="4" required></textarea>
				<br>
				<input class="border-solid border-2 border-black rounded-md p-1" type="submit" value="Submit">
			</form>
			<hr>
			{{#each comments}}
			<div>
				<p class="italic">From User {{ user_id }}:</p>
				<p>{{ content }}</p>
			</div>
			{{/each}}
		</div>
	</div>
</body>
</html>
<!DOCTYPE html>
<html lang="en">
<head>
	<title>{{ thread.title }}</title>
	<meta charset="UTF-8" />
	<link rel="stylesheet" type="text/css" href="/styles.css" />
	<meta name="viewport" content="width=device-width,initial-scale=1" />
</head>
<body>
	<div class="min-h-screen bg-slate-900 text-sky-100">
		<div class="min-h-screen w-5/6 md:w-2/3 lg:w-1/2 mx-auto pt-6 py-2 space-y-5">
			<div class="text-center w-full space-y-2">
				<h1 class="text-xl md:text-2xl lg:text-3xl font-bold">Javascript-less Forum</h1>
				<p>A simple anonymous forum.</p>
				<p class="italic">You are {{user_id}}</p>
			</div>
			<div class="space-y-2">
				<div class="w-full flow-root px-1">
					<h2 class="float-left text-center text-lg md:text-xl lg:text-2xl">Viewing Thread <span class="italic">{{ thread.thread_id }}</span> <span class="text-sm font-light">{{ num_comments }} comments</span></h2>
					<p class="float-right text-center underline"><a href="/">Back to Home</a></p>
				</div>
				<div class="w-full bg-slate-800 p-3 space-y-2 rounded-md shadow-md">
					<div>
						<h3 class="break-all font-semibold text-lg">{{ thread.title }} <span class="text-sm font-light">by {{ user_id }}</span></h3>
						<p class="text-sm italic">created on {{ thread.created }}, last updated {{ thread.last_updated }}</p>
					</div>
					<p class="whitespace-pre-wrap break-all">{{{ thread.content }}}</p>
					<details class="w-full space-y-2">
						<summary class="select-none">Comment</summary>
						<form class="w-full pl-1 space-y-2" action="/thread_comment_{{ thread.thread_id }}" method="post">
							<textarea name="content" placeholder="Write something here..." rows="4" class="pl-2 py-1 w-full md:w-2/3 bg-slate-700 rounded-md shadow-md" required></textarea>
							<br>
							<input class="px-2 py-1 bg-slate-700 font-semibold rounded-md shadow-md" type="submit" value="Submit">
						</form>
					</details>
				</div>
				{{#each comments}}
				<div class="w-5/6 ml-auto bg-slate-800 p-3 space-y-2 rounded-md shadow-md">
					<p class="text-sm italic">{{ user_id }} on {{ created }}</p>
					<p class="whitespace-pre-wrap break-all">{{{ content }}}</p>
				</div>
				{{/each}}
			</div>
		</div>
	</div>
</body>
</html>
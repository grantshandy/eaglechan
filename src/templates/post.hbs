<!DOCTYPE html>
<html lang="en">

<head>
	<title>Eaglechan: {{post.title}}</title>
	<meta charset="UTF-8" />
	<link rel="stylesheet" type="text/css" href="./styles.css" />
	<meta name="viewport" content="width=device-width,initial-scale=1" />
</head>

<body>
	<div class="min-h-screen bg-blue-800 text-white text-center space-y-5 font-serif">
		<div>
			<div class="p-3 flow-root align-middle">
				<h1 class="float-left font-bold text-2xl">Viewing Post</h1>
				<a class="float-right underline" href="/">Back To Home</a>
			</div>
			<hr>
		</div>
		<div class="w-3/4 mx-auto p-4 bg-blue-900 border-2 border-white flow-root">
			<h2 class="text-2xl font-semibold">{{post.title}}</h2>
			<p class="italic">author id: {{post.author}} | post id: {{post.id}}</p>
			<p class="mt-2 italic">{{post.content}}</p>
		</div>
	</div>
</body>

</html>